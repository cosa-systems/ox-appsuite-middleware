apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-user-guide.fullname" . }}-test-connection"
  labels:
    {{- include "core-user-guide.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "core-user-guide.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
